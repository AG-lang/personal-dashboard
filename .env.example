# 数据库配置（后端）
POSTGRES_URL="postgresql://username:password@localhost:5432/personal_dashboard"
# 可选：Serverless 环境推荐使用非连接池地址
# POSTGRES_URL_NON_POOLING=

## 认证密钥（后端）
# 生产环境务必设置为强随机字符串
SECRET_KEY="please-change-me-in-production"

## SQL 日志开关（后端）
# 本地调试可设为 true，生产建议 false
# SQL_ECHO=false

## 前端环境变量请放在 frontend/.env.local 或 frontend/.env.example
# 例如：frontend/.env.local 中设置
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel 部署时的环境变量
# POSTGRES_USER=
# POSTGRES_HOST=
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=